我已按照你的要求，将错误处理逻辑进一步分层：

**架构调整：**

1.  **API 层 (`src/x1ayu_rag/api/system_api.py`)**:
    *   新增了 `validate_chat_config()` 和 `validate_embedding_config()` 方法。
    *   这些方法负责调用底层的工厂类进行校验，捕获异常，并将其转换为标准化的 `(success, message)` 返回格式。这符合“错误到 API 层处理成提示词”的要求。

2.  **CLI 装饰器 (`src/x1ayu_rag/cli/decorators.py`)**:
    *   更新了装饰器逻辑，不再直接依赖底层 `LLMFactory`。
    *   现在调用 `SystemAPI` 的校验方法，根据返回的 `success` 状态决定是否继续执行。
    *   如果失败，直接打印 API 层返回的友好提示信息。

**流程图解：**
`CLI Command` -> `Decorator` -> `SystemAPI` (捕获异常 -> 转为 Message) -> `Decorator` (显示 Message)

这种设计使得 CLI 层更加纯粹，只负责展示；而业务逻辑和错误处理细节都被封装在了 API 层。
