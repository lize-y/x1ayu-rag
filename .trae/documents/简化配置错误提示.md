我已按照你的要求，移除了 CLI 错误提示中的 "Configuration Error:" 前缀。

**变更详情：**

1.  **CLI 装饰器 (`src/x1ayu_rag/cli/decorators.py`)**:
    *   修改了 `require_chat_config` 和 `require_embedding_config` 装饰器。
    *   现在的输出格式从 `[red]Configuration Error:[/red] {msg}` 简化为直接输出 `[red]{msg}[/red]`。

**效果：**
现在如果配置缺失，输出将直接是：
`Chat configuration is empty. Please run 'rag config' to set it up.`
（红色字体）
